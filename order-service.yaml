spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost:3306/e_commerce_order_db
    username: root
    password: Hemasri@7107

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 7082
  address: 0.0.0.0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: false
    hostname: localhost
